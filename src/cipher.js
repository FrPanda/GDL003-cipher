  // ..const textoCifrado = document.getElementById("cifrado");
cipher.encode(offset)
cipher.decode(offset)
  function cifrado(){
  	const textoIngresado = texto.value;
  	textoCifrado.value = textoIngresado.split('').map(c=>{
  		let mayus = (c === c.toUpperCase()) ? true;
		}
};
